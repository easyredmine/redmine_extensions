<%= form_for([redmine_extensions_engine, @easy_settings]) do |f| %>
  <%= render "easy_settings/#{@settings.plugin.id}", settings: @settings, form: f %>

  <%= f.submit class: 'button-2' %>
<% end %>
