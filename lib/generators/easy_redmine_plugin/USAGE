Description:
    The plugin generator creates stubs for a new easy redmine plugin.
    Plugin is prepared for use in Redmine and EasyRedmine as well.

Example:
    bundle exec rails generate easy_plugin NameOfNewPlugin
      create  plugins/easyproject/easy_plugins/name_of_new_plugin/app/controllers
      ...
