Description:
    The plugin generator creates stubs for a new Redmine plugin.
    Plugin is prepared for use in Redmine and Easy Redmine as well.

Example:
    bundle exec rails generate easy_redmine_plugin NameOfNewPlugin
      create  plugins/easyproject/easy_plugins/name_of_new_plugin/app/controllers
      ...

Help:
        bundle exec rails generate easy_redmine_plugin --help
